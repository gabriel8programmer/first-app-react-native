
import { StyleSheet } from "react-native"

export default StyleSheet.create({
    boxTitle: {
        alignItems: "center",
        justifyContent: "center",
        padding: 10
    },
    textTitle: {
        color: "#ff0043",
        fontSize: 24,
        fontWeight: "bold"
    }
})